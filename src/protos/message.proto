syntax = "proto3";

package message;

message Message {
  int64 room_id = 1;
  int64 message_id = 2;
  int64 author = 4;
  string text = 5;
}

service MessageService {
  rpc chatStream (stream Message) returns (stream Message);
}